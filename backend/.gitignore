# 1. Dependencies
node_modules/
package-lock.json # Optional: Some teams commit this, some don't. Keep it if you want strict versions.

# 2. Environment Variables (CRITICAL: Never commit secrets!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 4. Docker (If you map local folders for data)
mongo-data/
db-data/
.docker/

# 5. OS & Editor Junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp